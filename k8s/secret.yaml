apiVersion: v1
kind: Secret
metadata:
  name: mcp-pinot-secrets
type: Opaque
data:
  # Base64 encoded values - replace with your actual credentials
  # Example: echo -n "your-username" | base64
  username: ""  # Optional: Base64 encoded Pinot username
  password: ""  # Optional: Base64 encoded Pinot password  
  token: ""     # Optional: Base64 encoded Pinot token
