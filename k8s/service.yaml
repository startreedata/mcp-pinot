apiVersion: v1
kind: Service
metadata:
  name: mcp-pinot-service
  labels:
    app: mcp-pinot-server
spec:
  selector:
    app: mcp-pinot-server
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
  type: ClusterIP
